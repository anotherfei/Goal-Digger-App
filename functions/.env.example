# ─────────────────────────────────────────────────────────────────────────────
# Goal Digger – Firebase Functions environment template
#
# DO NOT commit .env — it is gitignored.
# Share this file; share the real values via your secret manager or password manager.
#
# SETUP (run once, then teammates never need this file locally):
#   firebase functions:secrets:set GEMINI_API_KEY
#
# LOCAL DEV only (emulator reads .env automatically):
#   Copy this file → .env and fill in real values.
# ─────────────────────────────────────────────────────────────────────────────

GEMINI_API_KEY=your_google_ai_studio_key_here
